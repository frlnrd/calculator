name: Calculator Agent

on:
  workflow_dispatch:

  issues:
    types:
      - opened

permissions:
  issues: write

jobs:
  analyse:
    runs-on: ubuntu-latest

    steps:
      - name: Debug context
        run: |
          echo "Repository=${{ github.repository }}"
          echo "Owner=${{ github.repository_owner }}"
          echo "Ref=${{ github.ref }}"

      - name: Debug workspace
        run: |
          pwd
          ls -la

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          echo "Pas de checkout, donc pas de requirements.txt disponible"

      - name: Debug event
        run: |
          echo "Event=${{ github.event_name }}"
`